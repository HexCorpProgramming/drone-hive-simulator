extends Spatial

class_name InteractableObject

var state = false

func interact(interacter = null): #Interactable objects don't always need to know who's using them, not unlike 7589.
	pass #Do something interactable.
	
	